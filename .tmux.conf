set-option -g default-shell "/bin/bash"
set -g default-terminal "screen-256color"
set -g history-limit 65535
set -g prefix2 ` # add another prefix (keep the default)
bind - splitw -v # add an bind (keep the default)
bind \\ splitw -h 
