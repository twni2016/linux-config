# all these commands save config in ~/.gitconfig
git config --global user.name "twni2016"
git config --global user.email "twni2016@gmail.com"
git config --global color.ui auto # make git ui (git status) display color
